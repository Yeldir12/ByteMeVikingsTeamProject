<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Quest Board</title>
    <link rel="icon" href="../public/images/Knight_thumbnail01.png" type="image/png" />

    <%- include('partials/head-tags') %>

    <style>
      body {
        background-image: url('/images/banner.jpg'); /* Change path as needed */
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
      }

      /* Header banner */
      .header-banner {
        
     
        width: 100%;
        height: 150px;
        margin-bottom: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        text-shadow: 0 2px 8px rgba(0, 0, 0, 0.65);
      }

      .hero-card {
        background: white;
        border-radius: 20px;
        padding: 40px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        animation: fadeIn 0.6s ease;
        font-size: 0.9em;
      }

      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
      }

      .hero-icon {
        font-size: 60px;
       color: #4a6cf7;
      }

      h1 {
        font-weight: 700;
      }

      h2 {
        font-weight: 500;
        color: #555;
      }

      .emoji-big {
        font-size: 1.2rem;
      }
      
    </style>
  </head>

  <body>
    <%- include('partials/header') %>

    <!-- New Fancy Header Banner -->
    <div class="header-banner">
      <h1 class="display-5 fw-bold">BraveQuests</h1>
    </div>

    <main class="home-main py-4">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-md-8 col-lg-6">

            <div class="hero-card">

              <% if (!user) { %>
              <p class="text-center text-danger fw-bold">
                You are NOT logged in. 👀
              </p>
              <% } %>

              <div class="text-center mb-4">
                <i class="fa-solid fa-scroll hero-icon"></i>
              </div>

              <h2 class="text-center mb-3">
                Welcome to <i class="fa-solid fa-note-sticky"></i>
                <span class="text-primary">BraveQuests</span>!
              </h2>

              <h4 class="text-center mb-4">
                Embark on quests, lend a heroic hand, and level up your character!
              </h4>

              <p class="fs-5">
                Whether you're rescuing a lost pet, delivering messages,
                or helping fellow adventurers conquer challenges,
                every heroic deed earns you valuable points.
              </p>

              <p class="fs-5">
                Spend your points to unlock abilities, upgrade gear,
                collect special outfits, and rise through the heroic ranks.
                The more you help others, the stronger your legend becomes!
                <span class="emoji-big">🛡️✨</span>
              </p>

            </div>

          </div>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>
